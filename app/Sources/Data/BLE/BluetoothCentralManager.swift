import CoreBluetooth
import Foundation

// Phase MVP — to be implemented
// Handles scan, connect, GATT service/characteristic discovery,
// notification subscription, and automatic reconnection with exponential backoff.
//
// Used by: TrainerService (FTMSAdapter, TacxAdapter), BLEHeartRateAdapter
